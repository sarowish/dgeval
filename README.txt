## Building the program

```
make
```

## Running the program

```
./project4 [optimization_parameter] <input_file>
```

The variable that points to the dynamically generated x64 code is named `func`
and a breakpoint can be set at `main.cpp` line 94.
